import type { ReactNode } from "react";
import MentorButton from "../../component/button.component";

export function MainPage(): ReactNode {
  return <>
    <aside>
      <MentorButton name='+' id="floating-button" onClick={() => {}}/>
    </aside>
  </>
}
